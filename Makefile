# запустить тестовую БД, для локального тестирования, при разработке.
runTestDb:
	docker-compose -f db_test/docker-compose.yml up

# Запустить два контейнера, с БД и приложением.
# Актуально использовать при развёртывании на прод или при предрелизном тестировании.
# Для настройки параметров запуска, смотреть в файл: .env
runProdApp:
	docker-compose up